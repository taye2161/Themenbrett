{ // Konfiguration von TypeScript für die Tests, die mit ts-jest ausgeführt werden.
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "noEmit": true,
    "types": [ "jest", "node" ],
  },
  "include": [
    "./**/*"
  ]
}