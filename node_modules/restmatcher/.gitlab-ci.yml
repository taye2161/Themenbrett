image: node:latest

stages:
  - test

javascript:
  stage: test
  script:
    - npm ci
    - npm run build
    - npm run test
  coverage: '/All files\s*|\s+([\d\.]+)\s*\|.*/'    
  artifacts:
    reports:
      junit:
        - junit.xml