"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.info = info;
exports.cmdInfo = cmdInfo;
const cmdfiles_1 = require("cmdfiles");
const WSStorageLocator_1 = require("./WSStorageLocator");
const StorageWSPath_1 = require("./StorageWSPath");
/**
 * Exports the VS Code Copilot chat history for a workspace.
 * @returns The full history as a string, or an empty string if no history was found.
 *   When `options.output` is specified the history is also written to that file.
 *   The history is never written to stdout by this function.
 * @throws {Error} if options are invalid or a Dev Container environment prevents discovery.
 */
async function info(options) {
    const workspaceFolder = options.workspace || process.cwd();
    const customVSCodeDataPath = options.vscodeDataPath ?? "";
    const noRemote = options.noRemote ?? false;
    const enforceRemote = options.enforceRemote ?? false;
    const remoteType = options.remoteType ?? "";
    const remotePath = options.remotePath ?? "";
    const caseSensitive = options.caseSensitive ?? false;
    const onlyLatest = options.onlyLatest ?? false;
    const quiet = options.quiet ?? true;
    if (quiet) {
        (0, cmdfiles_1.verbosity)({ quiet: true });
    }
    try {
        if (noRemote && enforceRemote) {
            throw new Error("Options noRemote and enforceRemote cannot be used together.");
        }
        const foundWorkspaceStorageFolders = await (0, WSStorageLocator_1.findWorkspaceStorageFolders)({ workspaceFolder, customVSCodeDataPath, noRemote, remoteType, remotePath, enforceRemote, caseSensitive, onlyLatest });
        if (foundWorkspaceStorageFolders.length === 0) {
            return "";
        }
        (0, cmdfiles_1.log)(`Found ${foundWorkspaceStorageFolders.length} workspace storage folder(s)`);
        const creationTimeOfWorkspacePath = (0, WSStorageLocator_1.getCreationTimeOfWorkspace)(workspaceFolder);
        for (const folder of foundWorkspaceStorageFolders) {
            (0, cmdfiles_1.log)(`- Storage folder: ${folder}`);
            try {
                const storageWSPath = StorageWSPath_1.StorageWSPath.fromWorkspaceStorage(folder, caseSensitive);
                if (storageWSPath) {
                    (0, cmdfiles_1.log)(`   - Storage's Workspace URI:  ${storageWSPath.encodedUriCased}`);
                    (0, cmdfiles_1.log)(`   - Creation time of storage: ${new Date(storageWSPath.ctime).toLocaleString()}`);
                }
            }
            catch (e) {
                (0, cmdfiles_1.log)(`   - Error reading workspace storage: ${e instanceof Error ? e.message : String(e)}`);
            }
        }
        return "";
    }
    finally {
        if (quiet) {
            (0, cmdfiles_1.restoreVerbosity)();
        }
    }
}
async function cmdInfo(options) {
    try {
        const outputFile = options.output;
        const content = await info({
            sessionsDir: options.sessionsDir,
            workspace: options.workspace,
            vscodeDataPath: options.vscodeDataPath,
            noRemote: Boolean(options.noRemote),
            enforceRemote: Boolean(options.enforceRemote),
            remotePath: options.remotePath,
            onlyLatest: Boolean(options.onlyLatest),
            quiet: Boolean(options.quiet),
        });
        process.stdout.write(content);
    }
    catch (e) {
        program.error(e instanceof Error ? e.message : String(e));
    }
}
//# sourceMappingURL=cmdInfo.js.map