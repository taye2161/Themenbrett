"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.StorageWSPath = exports.FILE = exports.VSCODE_REMOTE = exports.WORKSPACE_JSON_FILE = void 0;
exports.injectPathModuleForTesting = injectPathModuleForTesting;
const fs = __importStar(require("fs"));
const url = __importStar(require("url"));
const platformPathModule = __importStar(require("path"));
// enable injection of platform specific version (path.win32 and path.linux) in testing
let path = platformPathModule;
function injectPathModuleForTesting(pathModule) {
    path = pathModule;
}
exports.WORKSPACE_JSON_FILE = "workspace.json";
/**
 * The protocol used in the encoded URI in workspace.json for remote workspaces, e.g. "vscode-remote://dev-container+container-name/workspace/project" or "vscode-remote://wsl+ubuntu/home/alice/project"
 * For local workspaces, the encoded URI is like "file:///home/alice/project" or "file:///c%3A/Users/john/workspaces/project"
 */
exports.VSCODE_REMOTE = "vscode-remote:";
exports.FILE = "file:";
/**
 * cf. https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/encodeURIComponent#encoding_for_rfc3986
 */
function encodeRFC3986URIComponent(str) {
    return encodeURIComponent(str).replace(/[!'()*]/g, (c) => `%${c.charCodeAt(0).toString(16).toUpperCase()}`);
}
function pathToEncodedURL(file, sep, prefix = "file://") {
    return file
        .split(sep ?? path.sep)
        .reduce((acc, part) => part ? acc + "/" + encodeRFC3986URIComponent(part) : acc, prefix);
}
/**
 * In the workspace.json file in the storage folder, the folder name
 * of the workspace is stored as an encoded URI.
 * Besides the encoding, this class also handles the correct case:
 * If case is insensitive, the file paths are converted to lower case.
 */
class StorageWSPath {
    protocol;
    /**
     * Empty for file system, for remotes the part before the ":" of the host,
     * e.g., dev-container or wsl
     *
     */
    hostType;
    path;
    pathCased;
    encodedUriCased;
    basenameEncoded;
    /** Computed lazily in file path case */
    _ctime;
    /** Derived, computed lazily */
    _directoryCased = null;
    static fromFilePath(filePath, caseSensitive) {
        return new StorageWSPath({
            protocol: exports.FILE,
            hostType: "",
            path: filePath,
            pathCased: caseSensitive ? filePath : filePath.toLowerCase(),
            encodedUriCased: caseSensitive
                ? pathToEncodedURL(filePath)
                : pathToEncodedURL(filePath).toLowerCase(),
            basenameEncoded: caseSensitive
                ? encodeRFC3986URIComponent(path.basename(filePath))
                : encodeRFC3986URIComponent(path.basename(filePath).toLowerCase()),
            _ctime: null // lazy
        });
    }
    /**
     *
     * @throws Error if the workspaceStoragePath is not a valid VS Code workspace storage folder (e.g. missing workspace.json or missing "folder" property in workspace.json)
     */
    static fromWorkspaceStorage(workspaceStoragePath, caseSensitive) {
        const workspaceJsonPath = path.join(workspaceStoragePath, exports.WORKSPACE_JSON_FILE);
        if (!fs.existsSync(workspaceJsonPath)) {
            throw new Error("Invalid workspace storage folder, missing workspace.json: " + workspaceStoragePath);
        }
        const ctime = fs.statSync(workspaceJsonPath).ctimeMs;
        const raw = fs.readFileSync(workspaceJsonPath, "utf8");
        const obj = JSON.parse(raw);
        if (obj.folder) {
            return StorageWSPath.fromEncodedUri(obj.folder, ctime, caseSensitive);
        }
        else {
            throw new Error(`Invalid workspace.json file: ${workspaceJsonPath}, missing "folder" property`);
        }
    }
    /**
     * Only for testing, in real use, this is only called by fromWorkspaceStorage which reads the encoded URI from the workspace.json file.
     */
    static fromEncodedUri(encodedUri, ctime, caseSensitive) {
        const uri = new URL(encodedUri);
        const uriPath = decodeURIComponent(uri.pathname);
        const basename = path.basename(uriPath);
        const host = decodeURIComponent(uri.host);
        let ospath = uriPath;
        if (uri.protocol === exports.FILE) {
            try {
                // option only for testing windows paths on non-windows platforms
                ospath = url.fileURLToPath(encodedUri, { windows: path.sep === "\\" });
            }
            catch {
                // In case of error (e.g. on Windows if the path is not absolute), fallback to the decoded URI path here
                ospath = uriPath;
                if (path.sep === "\\") {
                    uriPath.split("/").join(path.sep); // convert to platform specific separators
                    if (ospath.startsWith("\\")) {
                        ospath = ospath.substring(1);
                    }
                }
            }
        }
        return new StorageWSPath({
            protocol: uri.protocol,
            hostType: host.split("+")[0], // for remotes, the host is like "dev-container+container-name" or "wsl+ubuntu" and we only want the "dev-container" part for matching
            path: ospath,
            pathCased: caseSensitive
                ? ospath
                : ospath.toLowerCase(),
            encodedUriCased: caseSensitive
                ? encodedUri
                : encodedUri.toLowerCase(),
            basenameEncoded: caseSensitive
                ? encodeRFC3986URIComponent(path.basename(basename))
                : encodeRFC3986URIComponent(path.basename(basename).toLowerCase()),
            _ctime: ctime // actually the ctime of the workspace folder
        });
    }
    constructor(data) {
        this.protocol = data.protocol;
        this.hostType = data.hostType;
        this.path = data.path;
        this.pathCased = data.pathCased;
        this.encodedUriCased = data.encodedUriCased;
        this.basenameEncoded = data.basenameEncoded;
        this._ctime = data._ctime;
    }
    get ctime() {
        if (this._ctime === null) {
            this._ctime = fs.statSync(this.path).ctimeMs;
        }
        return this._ctime;
    }
    get directoryCased() {
        if (this._directoryCased === null) {
            this._directoryCased = path.dirname(this.pathCased);
        }
        return this._directoryCased;
    }
    matchesRemoteType(remoteType) {
        return !remoteType || this.hostType === remoteType;
    }
    /**
     * Matches this file workspace path with the given pathFragmentCased.
     * The pathFragmentCased is either an absolute path (starting with "/") or a relative path.
     * If it is a relative path, it is matched with the end of the workspace path,
     * e.g. "dev" matches "/home/user/dev" and also "/workspace/remote/dev", but not "/home/user/developer".
     *
     * This is used to check if a workspace file path (usually from where the cli tool is called),
     * which basename already matches the workspace storage project name (basename),
     * also matches the remote path in the workspace storage (if specified).
     *
     * Since tis is only used to check paths in a remote container, only linux separators are supported here.
     */
    matchesPathFragment(pathFragmentCased) {
        // "vscode-remote://«remoteType»+«container-name»/«remotePath»/«targetProject»"
        if (!pathFragmentCased) {
            return true;
        }
        if (pathFragmentCased.startsWith("/")) {
            // absolute remote path matching
            return this.directoryCased === pathFragmentCased;
        }
        else {
            // relative remote path matching: check if path ends with remotePath
            return this.directoryCased.endsWith(pathFragmentCased);
        }
    }
}
exports.StorageWSPath = StorageWSPath;
//# sourceMappingURL=StorageWSPath.js.map