import type { ChatRequest } from "./types";
export declare const KIND_THINKING = "thinking";
export declare const NEWLINE = "\n";
export declare const REQUEST_SEPARATOR: string;
export declare function modelIdToDisplayName(modelId: string): string;
export declare function formatRequest(req: ChatRequest, modelName: string): string;
/**
 * Renders a single request/response exchange as GitHub-flavoured Markdown.
 *
 * Structure:
 *   ## [timestamp]
 *   ### User
 *   [user message]
 *   ### [model name]
 *   [thinking / invocation / response items — each a <details> collapsible]
 */
export declare function formatRequestMarkdown(req: ChatRequest, modelName: string): string;
//# sourceMappingURL=render.d.ts.map