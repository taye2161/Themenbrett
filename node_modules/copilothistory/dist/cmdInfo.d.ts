interface InfoOptions {
    /** Workspace folder to export history for (default: current working directory) */
    workspace?: string;
    /** Custom VS Code data directory (default: platform-specific standard path) */
    vscodeDataPath?: string;
    /** Read JSONL session files directly from this directory, bypassing VS Code workspace storage discovery */
    sessionsDir?: string;
    /** Do not search remote locations (Dev Container or WSL) as fallback when no direct workspace storage match is found */
    noRemote?: boolean;
    /** Enforce searching remote locations even when a direct workspace storage match is found */
    enforceRemote?: boolean;
    /** Prefix in remote container, e.g. "/home/dev" for absolute path "/home/dev/workspace" or "projects" for relative path matching all  "**/
    remoteType?: string;
    /** Absolute path to parent folder of workspace folder in the remote container */
    remotePath?: string;
    /** Compare files and paths in a case-sensitive manner */
    caseSensitive?: boolean;
    /** If multiple workspace storages are found, only use the latest one */
    onlyLatest?: boolean;
    /** Run quietly without printing verbose output -- when called programmatically, this is by default true. */
    quiet?: boolean;
}
/**
 * Exports the VS Code Copilot chat history for a workspace.
 * @returns The full history as a string, or an empty string if no history was found.
 *   When `options.output` is specified the history is also written to that file.
 *   The history is never written to stdout by this function.
 * @throws {Error} if options are invalid or a Dev Container environment prevents discovery.
 */
export declare function info(options: InfoOptions): Promise<string>;
export declare function cmdInfo(options: Record<string, unknown>): Promise<void>;
export {};
//# sourceMappingURL=cmdInfo.d.ts.map