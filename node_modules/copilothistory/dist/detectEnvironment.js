"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isInDevContainer = isInDevContainer;
exports.isWsl = isWsl;
exports.default = isInsideContainer;
exports.isDocker = isDocker;
const fs_1 = __importDefault(require("fs"));
const os_1 = __importDefault(require("os"));
const path_1 = __importDefault(require("path"));
/**
 * Just for hint in log output.
 */
function runsInDevContainer() {
    if (process.platform !== "linux") {
        return false;
    }
    const user = process.env.USER;
    return fs_1.default.existsSync(path_1.default.join(process.env.HOME ?? "/home/" + user, ".vscode-server"));
}
let isInDevContainerCached;
function isInDevContainer() {
    isInDevContainerCached ??= runsInDevContainer();
    return isInDevContainerCached;
}
// The following lines are copied (and only slightly changed) from the following micro-packages:
// - is-docker: https://github.com/sindresorhus/is-docker
// - is-inside-container: https://github.com/sindresorhus/is-inside-container
// - is-wsl: https://github.com/sindresorhus/is-wsl
// to avoid adding dependencies. License of all these packages is MIT
// is-wsl
function hasWSLProperties() {
    if (process.platform !== 'linux') {
        return false;
    }
    if (os_1.default.release().toLowerCase().includes('microsoft')) {
        return !isInsideContainer();
    }
    try {
        if (fs_1.default.readFileSync('/proc/version', 'utf8').toLowerCase().includes('microsoft')) {
            return !isInsideContainer();
        }
    }
    catch { }
    // Fallback for custom kernels: check WSL-specific paths.
    if (fs_1.default.existsSync('/proc/sys/fs/binfmt_misc/WSLInterop')
        || fs_1.default.existsSync('/run/WSL')) {
        return !isInsideContainer();
    }
    return false;
}
;
let isWslCached;
function isWsl() {
    isWslCached ??= hasWSLProperties();
    return isWslCached;
}
// is-inside-container
// Podman detection
const hasContainerEnv = () => {
    try {
        fs_1.default.statSync('/run/.containerenv');
        return true;
    }
    catch {
        return false;
    }
};
let isInsideContainerCached;
function isInsideContainer() {
    isInsideContainerCached ??= hasContainerEnv() || isDocker();
    return isInsideContainerCached;
}
// is-docker
function hasDockerEnv() {
    try {
        fs_1.default.statSync('/.dockerenv');
        return true;
    }
    catch {
        return false;
    }
}
function hasDockerCGroup() {
    try {
        return fs_1.default.readFileSync('/proc/self/cgroup', 'utf8').includes('docker');
    }
    catch {
        return false;
    }
}
function hasDockerMountInfo() {
    try {
        return fs_1.default.readFileSync('/proc/self/mountinfo', 'utf8').includes('/docker/containers/');
    }
    catch {
        return false;
    }
}
let isDockerCached;
function isDocker() {
    isDockerCached ??= hasDockerEnv() || hasDockerCGroup() || hasDockerMountInfo();
    return isDockerCached;
}
//# sourceMappingURL=detectEnvironment.js.map