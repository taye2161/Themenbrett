image: node:20

stages:
  - test

javascript:
  stage: test
  script:
    - npm ci
    - npm test
  coverage: '/All files\s*\|\s*([0-9.]+)\s*\|.*/'
  artifacts:
    reports:
      junit:
        - junit.xml