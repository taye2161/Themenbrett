export declare function execShellExec(cmd: string): Promise<void>;
export interface ShellColorOptions {
    colored: boolean;
    colorStandard?: string;
    colorSection?: string;
    colorSubsection?: string;
    colorError?: string;
}
interface ShellOutputOptions {
    indent: boolean;
    prefix: string;
}
interface ShellOptions extends ShellOutputOptions {
    /** Current working directory */ cwd?: string;
    env?: NodeJS.ProcessEnv;
}
/**
 * Executes a command via `process.spawn` and captures output while running.
 * The output may be formatted using the options:
 * - `indent`: if true, the output is indented
 * - `prefix`: if set, the prefix is added to every line
 */
export declare function execShell(cmd: string, args: string[], colorOptions: ShellColorOptions, options?: ShellOptions): Promise<unknown>;
/**
 * Executes a command via `process.spawn` and captures output while running.
 * The output may be formatted using the options:
 * - `indent`: if true, the output is indented
 * - `prefix`: if set, the prefix is added to every line
 */
export declare function execShellAndRetrieveOutput(cmd: string, args: string[], options: {
    cwd: string | undefined;
}): Promise<string[]>;
export declare const COLOR_START = "\u001B[";
export declare const COLOR_END = "m";
export declare const COLOR_RESET: string;
export declare function timestamp(): string;
export {};
//# sourceMappingURL=cliShell.d.ts.map