"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const commander_1 = require("commander");
const cmdZip_1 = require("./cmdZip");
const cmdfiles_1 = require("cmdfiles");
globalThis.program = new commander_1.Command();
(0, cmdfiles_1.verbosity)();
program.name('ziplocalgit')
    .description('Zip local git in order to create a student submission.')
    .version('1.0.0');
program.command('zip')
    .description('Create a zip file with local git (.git)')
    .option('--noCommitCheck', 'Do not check that all files are committed', false)
    .option('--noOverwrite', 'Do not overwrite existing submission zip', false)
    .option('-f --filename <filename>', 'Name of zip file, use ${CWD} for (simple) name of current folder.', 'abgabe_${CWD}.zip')
    .option('--minsize <minsize>', 'Emit warning if zip file size is less than min size (in kByte).', '20')
    .option('--maxsize <maxsize>', 'Emit warning if zip file size is greater than min size (in kByte).', '2000')
    .option('--addCopilotHistory', 'Add the file .copilot_history to the zip file.', false)
    .option('--noDevContainer', 'Do not search Dev Container locations as fallback when no direct workspace storage match is found (default: false)', false)
    .option('--enforceDevContainer', 'Enforce searching Dev Container locations even when a direct workspace storage match is found (default: false)', false)
    .option('--pathPrefixInDevContainer <string>', 'Absolute path to parent folder of workspace folder in the dev container, e.g "/workspace"; if empty, all matching workspaces in devcontainers are considered (default: "")', "")
    .option('--copilotHistoryFilename <copilotHistoryFilename>', 'Name of the file to add to the zip', 'copilot_chat_history.md')
    .option('--verbose', 'Verbose output', false)
    .option('--debug', 'Debug output', false)
    .option('--quiet', 'Quiet output', false)
    .action(cmdZip_1.cmdZip);
program.parse();
//////////////////////////////////////////////
//# sourceMappingURL=cli.js.map